MySQL的高级应用：
1、索引的优化
设置适合的字段属性值
索引使用最左前缀法则
不在索引上做任何操作
where语句后面的条件尽量都设为索引
尽量少使用like <> != null is not null
尽量使用覆盖语句
2、MySQL的主从复制
主从复制涉及主库和从库，主库负责写操作，从库负责读操作
还涉及undo log、redo log以及binlog
3、MySQL的事务级别以及四大特性
四大特性：
原子性(a)
一致性(c)
隔离性(i)
持久性(d)
事务级别：
读未提交
读已提交
可重复读
可串行
默认为可重复读
