7/20
1	日报
1.1	工作内容
1.1.1	内容
熟悉新学习的框架，以此为基础，复习servlet中session，文件上传、下载，图形验证码。
1.1.2	问题与解决
request与session中都有map关于request请求，生命周期为 ：发送连接请求——收到回复   至此request请求就失效了，放到里面的值也就取不到了；而session不同，只要session会话不结束，session里面的值会一直存在（在不同请求页面都要用到的值我们一般都放到session里）
servlet3.0文件上传
<form action="upload" method="post" enctype="multipart/form-data">
表单传参一定用post，后面属性设置不能丢
对应servlet里要配两个@
@MultipartConfig //将文件上传域封装为part对象
@WebServlet("/upload")
2	计划
2.1	工作内容
2.1.1	内容
1.小组面试
2.面试题：abstract，static,final，this,super关键字用法
3.面试题：new对象的初始化过程

输入一棵二叉树，求该树的深度。从根结点到叶结点依次经过的结点（含根、叶结点）形成树的一条路径，最长路径的长度为树的深度
public class Solution {
    public int TreeDepth(TreeNode root) {
     if(root==null)
         return 0;
        int data1=TreeDepth(root.left)+1;
        int data2=TreeDepth(root.right)+1;
        return data1>data2?data1:data2;
     
    }
}
给定一棵二叉搜索树，请找出其中的第k小的结点。例如， （5，3，7，2，4，6，8）    中，按结点数值大小顺序第三小结点的值为4。
解析：定义类外变量 t 用来存储所求结点，初始为null，先递归访左，一直到为null，然后return sum++ 访右，再访左为空 return sum++，只有当sum==k时，将root值赋给t



public class Solution {
    TreeNode t=null;
    int sum=0;
    TreeNode KthNode(TreeNode pRoot, int k)     
    {
        if(pRoot==null||k==0){
            return t;
        }        
        KthNode(pRoot.left,k);
        sum++;
        if(k==sum){
            t=pRoot;
            return t;
        }
        KthNode(pRoot.right,k);
        return t;       
    }
}
